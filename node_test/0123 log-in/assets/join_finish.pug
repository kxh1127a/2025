doctype html
html(lang="ko")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
    body 
        h1 회원가입이 완료되었습니다.
        h2 #{name}님, 반갑습니다.

        form(action="/login" method="get")
            input(type="submit" value=" 로그인 페이지 바로가기 ") 